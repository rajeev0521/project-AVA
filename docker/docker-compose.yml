version: '3.8'

services:
  ava:
    build: 
      context: ..
      dockerfile: docker/Dockerfile
    volumes:
      - ..:/app
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    ports:
      - "8000:8000"
    command: uvicorn ava.api.server:app --host 0.0.0.0 --port 8000

